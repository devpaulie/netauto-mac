# 변경 이력

*netauto — by devpaulie*

## 2.5.0

메뉴바 UI를 전면 재설계하고, IP 처리 방식을 선택할 수 있게 했습니다.

### 메뉴바 패널 재설계

`MenuBarExtra` 를 `.menu` 에서 **`.window` 스타일**로 바꿨습니다.
`.menu` 는 SwiftUI 를 실제 `NSMenu` 로 변환하는데, 그 과정에서 세 가지가 깨집니다:

- `Text` 항목이 전부 비활성(회색)으로 그려진다
- 공백으로 맞춘 정렬이 가변폭 글꼴에서 어긋난다
- SF Symbol 이 버려진다 (아이콘·체크 표시가 사라짐)

`.window` 는 일반 SwiftUI 뷰 계층이라 이 제약이 없습니다.

- 하위 메뉴를 없애고 프로필·IP 를 **한 화면에서 바로** 고르도록
- `Grid` 로 현재 상태 열 정렬 (공백 정렬 폐기)
- 배경을 **불투명**하게 — 반투명 재질은 뒤 창이 배어 나와 대비가 무너졌음
- 타입 스케일 정리 (헤더 15 / 항목 13 / 값 12 / 라벨 11)
- 상태별 강조색: 자동은 강조색, 수동 지정은 주황, 멈춤·꺼짐은 회색
- 규칙과 현재 상태가 다르면 `규칙` 줄을 주황색으로 표시
- 자동 전환 on/off 를 스위치로

### IP 처리 방식 선택

규칙마다 세 가지 중 고릅니다.

| | |
|---|---|
| **그대로 두기** | IP 설정을 건드리지 않음 |
| **자동 (DHCP)** | `networksetup -setdhcp` |
| **고정 IP** | `networksetup -setmanualwithdhcprouter` (ConfigMethod INFORM) |

- 고정 IP 는 **“자주 쓰는 IP” 목록에서 골라** 씁니다 (직접 타이핑 불필요)
- 패널에서도 자동(DHCP) / 저장된 주소를 바로 적용
- `netauto setdhcp` CLI 추가
- 예전 설정(`ip_mode` 없음)은 `ip` 값이 있으면 고정, 없으면 그대로로 추론

### 설정 창

- 프로필 **아이콘 선택 12종** + 메뉴바 미리보기
- **취소** 버튼 추가 — 변경이 있으면 확인 후 닫음 (`esc`)
- 되돌리기는 변경이 있을 때만 활성화, 저장 성공 시 창 닫힘
- 규칙 검증: 고정 IP 인데 주소를 안 고른 경우를 잡음

### 배포

- **유니버설 빌드** (arm64 + x86_64) — 인텔 맥에서도 동작
- 기본 아이콘 `wifi` → `globe` (시스템 Wi-Fi 아이콘과 겹치던 문제)
- 기본 설정을 **중립**으로 — 첫 설치 시 아무것도 바꾸지 않음, 회사 설정이 들어있지 않음
- dmg 안내문에 macOS 15 의 “확인 없이 열기” 경로와 네트워크 위치 만드는 순서 추가

### 고친 버그

- **pkg 가 `/Applications` 에 설치되지 않던 문제** — `pkgbuild` 가 페이로드의 `.app` 을
  재배치 가능으로 표시해, 설치 시 같은 번들 ID 의 기존 앱 위치(개발 중 실행해 본 복사본)에
  덮어썼다. `--component-plist` 로 `BundleIsRelocatable=false` 고정.
- **bash 3.2 파서 버그** — `$( … case … )` 안의 `case` 패턴 `)` 를 명령 치환의 닫는 괄호로
  오인한다. `bash -n` 은 명령 치환 내부를 파싱하지 않아 통과했고 실행에서만 드러났다.
  설명 문자열 생성을 헬퍼 함수로 분리.
- **설정 일부가 빠지면 전체 로드 실패** — Swift 자동 생성 디코더는 프로퍼티에 기본값이 있어도
  키가 없으면 실패한다. 관대한 `init(from:)` 을 직접 작성.
- **`Equatable` 이 `id` 까지 비교** — `id`(UUID)는 JSON 에 저장되지 않아 디코딩 때마다 새로
  생성된다. 내용만 비교하도록 `==` 직접 구현.
- **무한 재귀** — `FileManager.contents(atPath:)` 는 이미 `Data?` 를 반환하는데 같은 이름의
  확장을 만들어 자기를 호출했다.

### 테스트

83 (데몬 로직) + 17 (Swift ↔ bash 설정 왕복) = **100개**

---

## 2.0.0

- 설정을 **JSON** 으로 전환 (`/usr/local/etc/netauto/config.json`)
  — Swift 앱과 bash 데몬이 같은 파일을 공유. bash 쪽은 `plutil` 로 읽어 외부 의존성 없음
- **설정 창** 추가 — 규칙·자주 쓰는 IP·프로필 이름표를 GUI 에서 편집
- 메뉴바에 프로필 이름 표시 (`Office` / `Auto`)
- 규칙에 고정 IP 지정 기능

## 1.1.0

- **메뉴바 앱** 추가 (SwiftUI `MenuBarExtra`, Xcode 없이 빌드)
- GUI 가 비밀번호 없이 동작하도록 **요청 큐** 도입
  (launchd `QueueDirectories` → root 데몬이 수행)
- `netauto status --json`, `netauto pick <프로필>`
- `.pkg` + `.dmg` 설치 프로그램

## 1.0.0

- SSID 에 따라 macOS **네트워크 위치**를 자동 전환하는 데몬 + CLI
- 트리거: `/var/run/resolv.conf` 감시 + 30초 주기 안전망
- 멱등성: 이미 원하는 상태면 아무것도 쓰지 않음 (자기 트리거 루프 차단)
- 사용자가 직접 바꾼 프로필은 그 Wi-Fi 에 있는 동안 존중
- 일시중지 / 완전 비활성 / 로그
